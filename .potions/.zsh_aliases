alias python=python3

# VPN
alias ssh-activate="eval \`ssh-agent -s\` && ssh-add"
alias openvpn-stop-session='sudo killall -9 openvpn'

# TMUX
alias tmux='tmux -f ~/.potions/tmux/tmux.conf'

# Dev
alias be='bundle exec'
alias dc='docker-compose'
alias vim='nvim'

# Potions Plugin Management
alias potions-plugin='~/.potions/plugins.sh'
